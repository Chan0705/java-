const a = 1;
console.log('a 타입', typeof a);

const b = '1';
console.log('b 타입', typeof b);

const c = 2n; // 2^53 - 1 보다 큰 값

const d = true;

const e = null;

const f = undefined;

const g = NaN; // Not a Number

const h = Infinity;
